describe('SERVER TEST SUITE', () => {

    describe('Running a test', () => {

        it('should pass when running a test', () => {
            const expected = false;

            expect(false).toEqual(expected)
        });

    });

});
